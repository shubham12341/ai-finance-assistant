<svg width="64" height="64" viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">

  <!-- Background -->
  <rect width="64" height="64" rx="14" fill="#0f172a"/>

  <!-- Segment 1 — Blue (top right) -->
  <path d="M32 8 A24 24 0 0 1 56 32 L32 32 Z" fill="#378ADD"/>

  <!-- Segment 2 — Green (bottom right) -->
  <path d="M56 32 A24 24 0 0 1 32 56 L32 32 Z" fill="#1D9E75"/>

  <!-- Segment 3 — Yellow (bottom left) -->
  <path d="M32 56 A24 24 0 0 1 8 32 L32 32 Z" fill="#EF9F27"/>

  <!-- Segment 4 — Purple (top left) -->
  <path d="M8 32 A24 24 0 0 1 32 8 L32 32 Z" fill="#7F77DD"/>

  <!-- Dark center circle -->
  <circle cx="32" cy="32" r="13" fill="#0f172a"/>

  <!-- FA text in center -->
  <text x="32" y="36"
    text-anchor="middle"
    font-family="sans-serif"
    font-size="11"
    font-weight="700"
    fill="white">FA</text>

</svg>